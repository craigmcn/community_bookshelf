<% content_for :title, "Edit #{@series.name}" %>

<div class="row justify-content-center">
  <div class="col-md-7">
    <h1 class="mb-4">Edit Series</h1>
    <%= render "form", series: @series %>
  </div>
</div>
